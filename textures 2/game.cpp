#include <iostream>
#include "game.h"

using namespace std;
/*
vector should be empty initially 
vector holding icons 
add them and draw them when on level 3 (game level)
when on level 3, every time the player clicks on the screen, loop through the vector and check if any of them contain the x and y of the mouse

*/
